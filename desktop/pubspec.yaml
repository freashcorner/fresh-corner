name: fresh_corner_desktop
description: Fresh Corner Admin Desktop App (Ubuntu)
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  firebase_core: ^2.30.1
  firebase_auth: ^4.19.6
  cloud_firestore: ^4.17.4

  http: ^1.2.1
  provider: ^6.1.2
  cached_network_image: ^3.3.1
  intl: ^0.19.0
  data_table_2: ^2.5.13

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
